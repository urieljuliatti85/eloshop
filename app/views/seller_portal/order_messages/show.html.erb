<div class="mx-auto w-full max-w-3xl">
  <%= render "order_messages/conversation",
        order: @order,
        messages: @messages,
        message: @message,
        current_sender: Current.user,
        counterpart_name: @order.customer.name,
        back_path: seller_messages_path,
        form_url: seller_order_messages_path(@order) %>
</div>
