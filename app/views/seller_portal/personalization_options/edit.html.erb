<div class="mx-auto max-w-3xl space-y-6">
  <div><%= link_to "← #{@product.name}", seller_product_path(@product), class: "text-sm font-semibold text-brand-600" %><h1 class="mt-3 text-3xl font-bold">Editar personalização</h1></div>
  <%= render "form", product: @product, personalization_option: @personalization_option %>
</div>
