<div class="space-y-6">
  <header><%= link_to "← Voltar para o produto", admin_product_path(@product), class: "admin-link mb-3 inline-block text-sm" %><p class="admin-section-label mb-2">Personalização</p><h1>Editar campo</h1><p class="mt-2 text-sm text-slate-500"><%= @product.name %></p></header>
  <%= render "form", product: @product, personalization_option: @personalization_option %>
</div>
