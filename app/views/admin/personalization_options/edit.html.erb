<div class="w-full max-w-xl">
  <h1 class="text-2xl font-bold mb-6">Editar campo de personalização — <%= @product.name %></h1>

  <%= render "form", product: @product, personalization_option: @personalization_option %>
</div>
