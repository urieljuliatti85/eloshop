<div class="space-y-6">
  <header>
    <%= link_to "← Voltar para o produto", admin_product_path(@product), class: "admin-link mb-3 inline-block text-sm" %>
    <p class="admin-section-label mb-2">Catálogo</p>
    <h1>Editar produto</h1>
  </header>
  <%= render "form", product: @product %>
</div>
