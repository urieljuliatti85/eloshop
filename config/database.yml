# PostgreSQL. Versions 9.5 and up are supported.
#
# Install the pg driver:
#   gem install pg
# On macOS with Homebrew:
#   gem install pg -- --with-pg-config=/opt/homebrew/bin/pg_config
# On Windows:
#   gem install pg
#       Choose the win32 build.
#       Install PostgreSQL and put its /bin directory on your path.
#
# Configure Using Gemfile
# gem "pg"
#
default: &default
  adapter: postgresql
  encoding: unicode
  # For details on connection pooling, see Rails configuration guide
  # https://guides.rubyonrails.org/configuring.html#database-pooling
  max_connections: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>

  # Aponta para o serviço Postgres definido em docker-compose.yml (ver Fase 0
  # do ROADMAP.md). Os valores default cobrem o ambiente local via Docker;
  # em CI/produção, sobrescreva via variáveis de ambiente.
  host: <%= ENV.fetch("DATABASE_HOST", "localhost") %>
  port: <%= ENV.fetch("DATABASE_PORT", 5432) %>
  username: <%= ENV.fetch("DATABASE_USERNAME", "eloshop") %>
  password: <%= ENV.fetch("DATABASE_PASSWORD", "eloshop") %>

development:
  <<: *default
  database: eloshop_development

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  <<: *default
  database: eloshop_test

# As with config/credentials.yml, you never want to store sensitive information,
# like your database password, in your source code. If your source code is
# ever seen by anyone, they now have access to your database.
#
# Instead, provide the password or a full connection URL as an environment
# variable when you boot the app. For example:
#
#   DATABASE_URL="postgres://myuser:mypass@localhost/somedatabase"
#
# If the connection URL is provided in the special DATABASE_URL environment
# variable, Rails will automatically merge its configuration values on top of
# the values provided in this file. Alternatively, you can specify a connection
# URL environment variable explicitly:
#
#   production:
#     url: <%= ENV["MY_APP_DATABASE_URL"] %>
#
# Connection URLs for non-primary databases can also be configured using
# environment variables. The variable name is formed by concatenating the
# connection name with `_DATABASE_URL`. For example:
#
#   CACHE_DATABASE_URL="postgres://cacheuser:cachepass@localhost/cachedatabase"
#
# Read https://guides.rubyonrails.org/configuring.html#configuring-a-database
# for a full overview on how database connection configuration can be specified.
#
# Fase 20 (deploy): Railway fornece um único Postgres (DATABASE_URL) — cada
# papel (primary/cache/queue/cable) usa o mesmo host/porta/usuário/senha,
# mas com um banco próprio, sobrescrevendo só o nome. db:prepare precisa de
# bancos fisicamente distintos por papel pra carregar o schema de cada um
# corretamente (testado localmente: `url:` sozinho — mesmo com `database:`
# também presente no hash — sempre vence pro nome do banco, então reaproveitar
# a mesma DATABASE_URL nos 4 papéis faz db:prepare pular cache/queue/cable,
# já achando o banco "pronto" por causa do primary; por isso o parse manual
# abaixo, host/porta/usuário/senha explícitos em vez de `url:`). São 4 bancos
# dentro da mesma instância Postgres da Railway, não 4 instâncias — sem
# custo/complexidade extra de provisionar.
<% if ENV["DATABASE_URL"] %>
  <% database_url = URI.parse(ENV["DATABASE_URL"]) %>
<% end %>
production:
  primary: &primary_production
    <<: *default
    host: <%= database_url&.host %>
    port: <%= database_url&.port %>
    username: <%= database_url&.user %>
    password: <%= database_url&.password %>
    database: <%= database_url&.path&.delete_prefix("/") %>
  cache:
    <<: *primary_production
    database: <%= database_url&.path&.delete_prefix("/") %>_cache
    migrations_paths: db/cache_migrate
  queue:
    <<: *primary_production
    database: <%= database_url&.path&.delete_prefix("/") %>_queue
    migrations_paths: db/queue_migrate
  cable:
    <<: *primary_production
    database: <%= database_url&.path&.delete_prefix("/") %>_cable
    migrations_paths: db/cable_migrate
